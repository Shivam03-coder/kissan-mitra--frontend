import React, { useMemo } from "react";

const UserProfile = () => {
  return (
    <div
      className={`size-12 border-2 bg-green-500 border-black rounded-full font-Varela uppercase text-black text-3xl flex-center`}
    >
      S
    </div>
  );
};

export default UserProfile;
